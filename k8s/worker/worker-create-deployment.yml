apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-create
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker-create
  template:
    metadata:
      labels:
        app: worker-create
    spec:
      containers:
      - name: worker-create
        image: workercreate
        ports:
        - containerPort: 80
        env:
        - name: ASPNETCORE_URLS
          value: "http://+:80"
---
apiVersion: v1
kind: Service
metadata:
  name: worker-create
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: worker-create
