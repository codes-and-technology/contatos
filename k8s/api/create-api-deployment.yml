apiVersion: apps/v1
kind: Deployment
metadata:
  name: create-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: create-api
  template:
    metadata:
      labels:
        app: create-api
    spec:
      containers:
      - name: create-api
        image: createapi
        ports:
        - containerPort: 80
        env:
        - name: ASPNETCORE_URLS
          value: "http://+:80"
---
apiVersion: v1
kind: Service
metadata:
  name: create-api
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: create-api
